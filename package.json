{
    "name": "book_service_testing",
    "version": "1.0.0",
    "description": "Tests with cypress ",
    "main": "index.js",
    "scripts": {
        "chrome1": "npx cypress run --config-file cypress.json --browser chrome",
        "chrome2": "npx cypress run --config-file cypress.json --browser chrome --headed",
        "electron1": "npx cypress run --config-file cypress.env.json --browser electron",
        "electron2": "npx cypress run --config-file cypress.env.json --browser electron --headed"
    },
    "keywords": [
        "test",
        "cypress"
    ],
    "author": "Dmitry",
    "license": "ISC",
    "dependencies": {
        "cypress": "^9.5.1"
    },
    "devDependencies": {
        "cypress-xpath": "^1.6.2"
    }
}
